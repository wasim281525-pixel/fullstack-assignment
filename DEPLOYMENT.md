# Deployment Guide

Step 1: Clone repo
git clone https://github.com/soulpage/fullstack-assignment.git

Step 2: Start project
docker-compose up --build -d

Step 3: Access apps
Next.js -> http://SERVER_IP:3000
Django -> http://SERVER_IP:8000
